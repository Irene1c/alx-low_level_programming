#include "main.h"
#include <stdio.h>

/**
 * main - Prints its name
 * @argc: Argument count
 * @argv: Array pointing to the arguments
 *
 * Return: zero
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
